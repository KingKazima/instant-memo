# 产品需求文档 (PRD - Product Requirements Document)

**项目名称**：即刻备忘 (InstantMemo)  
**版本**：V1.0.0  
**文档状态**：定稿  
**核心哲学**：**“零摩擦捕获思维，像呼吸一样自然的记录。”**

### 1. 项目背景与目标
- **背景**：在快节奏的工作生活中，灵感、待办事项和临时想法稍纵即逝。现有笔记应用（如Notion、印象笔记）启动慢、结构复杂，导致记录成本过高。
- **目标**：打造一个**启动即写、写完即走、跨端同步**的极简备忘工具。将记录动作压缩到“打开网页→敲字→回车”三步内，总耗时不超过 3 秒。

### 2. 用户画像与场景
- **用户**：开发者本人（单用户模式），具备轻度多端（手机/平板/PC）使用习惯。
- **核心场景**：
    1. 开会时随手记下待回复的要点。
    2. 深夜灵感闪现，快速摸黑记下关键词。
    3. 在PC端复制了一段资料，同步到手机上稍后查看。

### 3. 功能需求 (Functional Requirements)

| 模块 | 功能点 | 交互细节（验收标准） |
| :--- | :--- | :--- |
| **录入页 (首页)** | 核心输入 | 页面中央展示巨大单行输入框，**自动获取焦点**（打开页面即可打字）。无标题栏、无分类标签、无富文本工具栏。 |
| | 提交动作 | 支持点击“记录”按钮或直接按下键盘 **Enter / 回车键** 提交。 |
| | 提交反馈 | 提交成功后，输入框内容**立即清空**并重新自动聚焦；屏幕底部闪现绿色“✓ 已记录”提示，1.6秒后自动淡出。 |
| | 页面跳转 | 首页右下角或底部提供极简文字链“查看全部 →”，点击进入列表页。 |
| **列表页 (查看)** | 数据展示 | 以**时间倒序**（最新的在最上）展示所有历史条目。单条条目展示内容（左）与创建时间（右）。 |
| | 信息密度 | 无分页、无“加载更多”，一次性拉取所有数据（个人数据量预估在千条以内，无性能压力）。 |
| | 返回交互 | 左上角“← 返回”按钮返回录入页；键盘按下 `ESC` 键亦可返回录入页。 |
| **数据持久化** | 云同步 | 所有数据存储于云端数据库，更换设备（手机/平板/PC）打开同一网页链接，数据**自动全量同步**。 |
| | 用户鉴权 | 采用**免密匿名登录**机制，用户打开即用，无需注册邮箱或手机号。 |

### 4. 非功能需求 (Non-Functional Requirements)

- **性能**：首屏加载时间（FCP）< 1.5秒；录入响应（提交到反馈）< 500ms。
- **兼容性**：兼容 Chrome/Safari/Firefox 最新两个版本；移动端适配良好（响应式布局），输入框不被手机键盘遮挡。
- **可用性**：在没有网络的情况下，优雅提示连接失败，不丢失已输入的内容（提交时做乐观重试）。
- **成本**：月度活跃用户（MAU）仅1人，云服务资源消耗必须控制在**免费额度**之内。

### 5. 约束与不做什么（V1.0 明确不做）
- ❌ 不支持富文本（加粗、图片、附件）。
- ❌ 不支持编辑和删除（若写错，可再记一条“更正：xxx”；保证数据只增不减，降低心智负担）。
- ❌ 不支持定时提醒推送（`remind_at` 字段预留但前端不实现）。